<Window
    x:Class="RepartoCopier.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="RepartoCopier">

    <Grid x:Name="Root" Background="Transparent">
        <Grid.Resources>
            <Style x:Key="SurfaceCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource LayerFillColorDefaultBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="8" />
            </Style>
            <Style x:Key="CompactButtonStyle" TargetType="Button">
                <Setter Property="Height" Value="30" />
                <Setter Property="MinWidth" Value="64" />
                <Setter Property="Padding" Value="9,0" />
                <Setter Property="CornerRadius" Value="6" />
            </Style>
            <Style x:Key="MenuButtonStyle" TargetType="Button">
                <Setter Property="Height" Value="40" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="Padding" Value="11,0" />
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Grid.Row="0" Title="RepartoCopier">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="ms-appx:///Assets/AppLogo.png" />
            </TitleBar.IconSource>
            <TitleBar.RightHeader>
                <Button x:Name="AppMenuButton" Width="30" Height="28" Padding="0" CornerRadius="6" ToolTipService.ToolTip="Menú">
                    <FontIcon Glyph="&#xE700;" FontSize="16" />
                    <Button.Flyout>
                        <Flyout Placement="BottomEdgeAlignedRight">
                            <Border Width="208" Padding="6" CornerRadius="8" Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                                <StackPanel Spacing="1">
                                    <Button Style="{StaticResource MenuButtonStyle}" Click="LoadProfile_Click"><StackPanel Orientation="Horizontal" Spacing="10"><FontIcon Glyph="&#xE8E5;" FontSize="16"/><TextBlock Text="Cargar copia..." /></StackPanel></Button>
                                    <Button Style="{StaticResource MenuButtonStyle}" Click="SaveProfile_Click"><StackPanel Orientation="Horizontal" Spacing="10"><FontIcon Glyph="&#xE74E;" FontSize="16"/><TextBlock Text="Guardar copia..." /></StackPanel></Button>
                                    <Rectangle Height="1" Margin="6,4" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />
                                    <Button Style="{StaticResource MenuButtonStyle}" Click="Settings_Click"><StackPanel Orientation="Horizontal" Spacing="10"><FontIcon Glyph="&#xE713;" FontSize="16"/><TextBlock Text="Ajustes" /></StackPanel></Button>
                                    <Button x:Name="AboutMenuButton" Style="{StaticResource MenuButtonStyle}" Click="About_Click"><StackPanel Orientation="Horizontal" Spacing="10"><FontIcon Glyph="&#xE946;" FontSize="16"/><TextBlock Text="Acerca de" /></StackPanel></Button>
                                    <Rectangle Height="1" Margin="6,4" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />
                                    <Button Style="{StaticResource MenuButtonStyle}" Click="Exit_Click"><StackPanel Orientation="Horizontal" Spacing="10"><FontIcon Glyph="&#xE8BB;" FontSize="16"/><TextBlock Text="Salir" /></StackPanel></Button>
                                </StackPanel>
                            </Border>
                        </Flyout>
                    </Button.Flyout>
                </Button>
            </TitleBar.RightHeader>
        </TitleBar>

        <Grid Grid.Row="1" Margin="4,0,4,2" MaxWidth="720" HorizontalAlignment="Stretch">
            <InfoBar x:Name="ErrorBar" VerticalAlignment="Top" Margin="0,0,0,8" IsOpen="False"
                     Severity="Error" IsClosable="True" Canvas.ZIndex="10" />

            <Grid x:Name="PreparationPanel" Visibility="Visible" RowSpacing="4" MaxWidth="712" HorizontalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,2">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel Spacing="1">
                        <TextBlock Text="Nueva copia" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="Un origen · varios destinos" Opacity="0.62" FontSize="11" />
                    </StackPanel>
                    <Button x:Name="StartButton" Grid.Column="1" Style="{StaticResource AccentButtonStyle}"
                            Height="36" MinWidth="126" CornerRadius="6" Click="Start_Click" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Spacing="7"><FontIcon Glyph="&#xE768;" FontSize="13"/><TextBlock Text="Iniciar copia"/></StackPanel>
                    </Button>
                </Grid>

                <Border Grid.Row="1" Style="{StaticResource SurfaceCardStyle}" Padding="6">
                    <Grid RowSpacing="4">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="1" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBox x:Name="SourcePathBox" Height="32" PlaceholderText="Archivo o carpeta de origen" IsReadOnly="True" CornerRadius="6" />
                            <Button Grid.Column="1" Style="{StaticResource CompactButtonStyle}" Click="PickSourceFile_Click" ToolTipService.ToolTip="Elegir archivo">
                                <StackPanel Orientation="Horizontal" Spacing="6"><FontIcon Glyph="&#xE8A5;" FontSize="13"/><TextBlock Text="Archivo"/></StackPanel>
                            </Button>
                            <Button Grid.Column="2" Style="{StaticResource CompactButtonStyle}" Click="PickSourceFolder_Click" ToolTipService.ToolTip="Elegir carpeta">
                                <StackPanel Orientation="Horizontal" Spacing="6"><FontIcon Glyph="&#xE8B7;" FontSize="13"/><TextBlock Text="Carpeta"/></StackPanel>
                            </Button>
                        </Grid>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                                <FontIcon Glyph="&#xEDA2;" FontSize="14" Opacity="0.72"/>
                                <TextBlock Text="Destinos" FontSize="13" FontWeight="SemiBold"/>
                                <TextBlock x:Name="DestinationCountText" Text="0 destinos" Opacity="0.58" FontSize="11" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4">
                                <Button Height="28" Padding="9,0" CornerRadius="6" Click="AddDestinations_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="5"><FontIcon Glyph="&#xE710;" FontSize="12"/><TextBlock Text="Agregar"/></StackPanel>
                                </Button>
                                <Button Width="28" Height="28" Padding="0" CornerRadius="6" Click="ClearDestinations_Click" ToolTipService.ToolTip="Quitar todos">
                                    <FontIcon Glyph="&#xE74D;" FontSize="12"/>
                                </Button>
                            </StackPanel>
                        </Grid>

                        <Rectangle Grid.Row="2" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />

                        <ListView x:Name="DestinationList" Grid.Row="3" SelectionMode="None" Height="34" MaxHeight="34"
                                  Padding="0" IsTabStop="False"
                                  ScrollViewer.HorizontalScrollMode="Enabled"
                                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                  ScrollViewer.VerticalScrollMode="Disabled"
                                  ScrollViewer.VerticalScrollBarVisibility="Disabled">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsStackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0,0,2,0"/>
                                    <Setter Property="MinHeight" Value="30"/>
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border MinWidth="56" MaxWidth="148" Height="30" Padding="5,0,2,0" CornerRadius="5"
                                            Background="{ThemeResource ControlFillColorDefaultBrush}"
                                            BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="1"
                                            ToolTipService.ToolTip="{Binding Path}">
                                        <Grid ColumnSpacing="3">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                            <FontIcon Glyph="&#xEDA2;" FontSize="12" Opacity="0.68" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Path}" FontSize="11" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" MinWidth="18" MaxWidth="92"/>
                                            <Button Grid.Column="2" Width="18" Height="18" Padding="0" CornerRadius="4" Tag="{Binding Path}" Click="RemoveDestination_Click" ToolTipService.ToolTip="Quitar">
                                                <FontIcon Glyph="&#xE711;" FontSize="9"/>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <Expander Grid.Row="4" Header="Opciones" IsExpanded="False" MinHeight="28" HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" Spacing="10" Padding="0,3,0,0">
                                <CheckBox x:Name="SkipSameCheck" Content="Omitir iguales"/>
                                <CheckBox x:Name="KeepGoingCheck" Content="Continuar ante error"/>
                            </StackPanel>
                        </Expander>
                    </Grid>
                </Border>

            </Grid>

            <Grid x:Name="RunningPanel" Visibility="Collapsed" RowSpacing="4" MaxWidth="712" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="0,4,0,0">
                <Border Style="{StaticResource SurfaceCardStyle}" Padding="6,5">
                    <Grid RowSpacing="6">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <Border Width="32" Height="32" CornerRadius="7" Background="{ThemeResource ControlFillColorDefaultBrush}" VerticalAlignment="Center">
                                <FontIcon x:Name="OperationIcon" Glyph="&#xE8A5;" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Spacing="0" VerticalAlignment="Center">
                                <TextBlock x:Name="OperationTitleText" Text="Copiando..." FontSize="12" Opacity="0.66"/>
                                <TextBlock x:Name="CurrentFileText" Text="Preparando..." FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxLines="1"/>
                                <TextBlock x:Name="CurrentPathText" Opacity="0.52" FontSize="10" TextTrimming="CharacterEllipsis" MaxLines="1"/>
                            </StackPanel>
                            <TextBlock x:Name="OverallPercentText" Grid.Column="2" Text="0%" FontSize="21" FontWeight="SemiBold" VerticalAlignment="Center" />
                        </Grid>

                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <ProgressBar x:Name="OverallProgressBar" Height="10" MinHeight="10" Minimum="0" Maximum="100" Value="0">
                                <ProgressBar.Resources>
                                    <x:Double x:Key="ProgressBarTrackHeight">10</x:Double>
                                </ProgressBar.Resources>
                            </ProgressBar>
                            <TextBlock x:Name="OverallDetailText" Grid.Row="1" Margin="0,3,0,0" Text="Preparando..." Opacity="0.58" FontSize="10"/>
                        </Grid>

                        <Grid Grid.Row="2" ColumnSpacing="16">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Velocidad fuente" Opacity="0.52" FontSize="10"/><TextBlock x:Name="SpeedMetricText" Text="0 MiB/s" FontSize="14" FontWeight="SemiBold"/></StackPanel>
                            <StackPanel Grid.Column="1"><TextBlock Text="Restante" Opacity="0.52" FontSize="10"/><TextBlock x:Name="RemainingMetricText" Text="--:--:--" FontSize="14" FontWeight="SemiBold"/></StackPanel>
                            <StackPanel Grid.Column="2"><TextBlock Text="Archivos" Opacity="0.52" FontSize="10"/><TextBlock x:Name="FilesMetricText" Text="0/0" FontSize="14" FontWeight="SemiBold"/></StackPanel>
                            <CheckBox x:Name="ShutdownCheck" Grid.Column="3" Content="Apagar al terminar" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>

                        <Grid Grid.Row="3" ColumnSpacing="7">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <Button x:Name="PauseButton" Style="{StaticResource CompactButtonStyle}" MinWidth="96" IsEnabled="False" Click="Pause_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6"><FontIcon x:Name="PauseIcon" Glyph="&#xE769;" FontSize="12"/><TextBlock x:Name="PauseButtonText" Text="Pausar"/></StackPanel>
                            </Button>
                            <Button x:Name="CancelButton" Grid.Column="1" Style="{StaticResource CompactButtonStyle}" MinWidth="96" IsEnabled="False" Click="Cancel_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6"><FontIcon Glyph="&#xE711;" FontSize="12"/><TextBlock Text="Cancelar"/></StackPanel>
                            </Button>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <Border Grid.Row="2" Padding="6,0" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid>
                <TextBlock x:Name="StatusText" Text="Listo" FontSize="10" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="ElapsedText" HorizontalAlignment="Right" FontSize="10" Opacity="0.62" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
